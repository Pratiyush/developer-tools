/**
 * Pure transformation for this tool. Replace with the real algorithm —
 * keep it side-effect-free so it can be unit-tested in isolation.
 */
export function process(input: string): string {
  return input.toUpperCase();
}
